{
  "name": "stadiumpulse-backend",
  "version": "1.0.0",
  "description": "StadiumPulse - GenAI Operations Copilot backend (FIFA World Cup 2026 Smart Stadiums challenge)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js",
    "lint": "eslint .",
    "typecheck": "tsc --allowJs --checkJs --noEmit --strict --target ES2022 --module ES2022 --moduleResolution bundler services/decisionEngine.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
